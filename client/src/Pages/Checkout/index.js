import DeliveryInfo from './DeliveryInfo';
import { BigContainer } from './checkoutStyle'

const Checkout = () => {
    return (
        <BigContainer>
            <DeliveryInfo />
            <div >test</div>
        </BigContainer>
    );
};

export default Checkout;
